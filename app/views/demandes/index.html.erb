<div class="row">
  <div class="col-md-12">
    <div class="">
      <%= link_to 'Nouvelle demande', new_demande_path, class: 'btn btn-success' %>
    </div>
    <div class="">
      <table class="table table-stripped">
        <thead>
          <tr>
            <th>Debut </th>
            <th>Fin </td>
            <th>Nombre de jours </th>
            <th>Type de congé </th>
            <th>Actions</th>
          </tr>
        </thead>
        <% @demandes.each do |demande| %>
        <tr>

          <td><%= demande.debut.strftime('%d %B %Y') %></td>
          <td><%= demande.fin.strftime('%d %B %Y') %></td>
          <td><%= demande.nb_jours %></td>
          <td><%= demande.type_conges %></td>
          <td><%= link_to 'Editer', demandes_path, class: 'glyphicon glyphicon-pencil' %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
